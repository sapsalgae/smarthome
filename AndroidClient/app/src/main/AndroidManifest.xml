<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.sweethome">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlarmActivity" />
        <activity android:name=".AlarmListActivity" />
        <activity android:name=".AlarmRingActivity" />
        <activity android:name=".TestActivity" />

        <service
            android:name=".ConnectionService"
            android:enabled="true"
            android:exported="true" />

        <receiver android:name=".AlarmReceiver" />

        <activity android:name=".AnalogOnOffActivity" />
        <activity
            android:name=".Raspberry.DoorlockActivity"
            android:label="@string/title_activity_doorlock"
            android:theme="@style/AppTheme" />

        <service
            android:name=".RaspberryService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".SocketConnection.ConnectionService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".SocketConnection.ConnectService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>